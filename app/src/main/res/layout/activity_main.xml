<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <EditText
        android:id="@+id/et_num_turns"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Number of turns" />

    <EditText
        android:id="@+id/et_code_length"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Length of code"
        app:layout_constraintTop_toBottomOf="@id/et_num_turns" />

    <Button
        android:id="@+id/btn_generate_game"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Generate Game"
        app:layout_constraintTop_toBottomOf="@id/et_code_length" />

</android.support.constraint.ConstraintLayout>